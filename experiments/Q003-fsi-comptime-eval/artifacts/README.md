# Q003 artifacts

One console project, `fsi-in-process-falsifier/`, pinned to `FSharp.Compiler.Service` 43.9.101
(`FsiEvaluationSession` ships in the same package — no extra dependency). Six staged checks; see
`../02-results.md` for the run.

```
cd fsi-in-process-falsifier && dotnet run
```
